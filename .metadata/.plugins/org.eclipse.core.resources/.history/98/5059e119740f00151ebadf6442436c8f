package Vjezbe;

public class Task2 {

	public static void main(String[] args) {

		TextIO.readFile("src/Vjezbe/niz.txt");

		String numAsString = TextIO.getln();
		String[] parts = numAsString.split(" ");
		String rows = parts[0];
		String columns = parts[1];
		int rowsAsNum = Integer.parseInt(rows);
		int columnsAsNum = Integer.parseInt(columns);
		// System.out.println(rows+" "+columns);
		String[][] num = new String[rowsAsNum][columnsAsNum];
		
		
		
		
		
		while (!TextIO.eof()) {
		String numAsString1 = TextIO.getln();	
		String[] parts1 = numAsString.split(" ");
		String rowValues = parts1[0];
		String columns1 = parts1[1];
		for (int i = 0; i < numAsString1.length()-1; i++) {
			rowValues=parts1[i];
			System.out.println(rowValues);
			
		}
//		int rowsAsNum1 = Integer.parseInt(rows1);
//		int columnsAsNum1 = Integer.parseInt(columns1);
//			
//			System.out.println(rows1);
//			System.out.println(columns1);
//			
//			for (int i = 0; i < num.length; i++){
//				for (int j = 0; j < num[i].length; j++) {
//
//					rows1 = parts[i];
//					num[i][j]=rows1;
//					System.out.println(num[i][j]);
//				}
//				System.out.println();
//			}
		}
	

	}

}
